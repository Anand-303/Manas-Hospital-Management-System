# MongoDB is schema-less, so we do not define models as with SQLAlchemy.
# You can use helper functions here for MongoDB operations if needed.

# Example usage in routes:
# mongo.db.users, mongo.db.doctors, etc. 